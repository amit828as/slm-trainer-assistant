# Training

Training execution is intentionally not implemented yet.

This directory will eventually hold backend adapters and run scripts, but the current milestone is to stabilize schemas, evals, validation, and baseline reporting first.

See `docs/training_backends.md` for the intended Unsloth and TRL + PEFT paths.
