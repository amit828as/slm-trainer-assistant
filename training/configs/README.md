# Training Configs

These files are placeholders that describe likely first-run settings. They should not be treated as proven hyperparameters.

Exact settings depend on the base model, tokenizer, prompt template, dataset size, target behavior, hardware, and evaluation results.
