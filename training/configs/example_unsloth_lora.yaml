# Placeholder only. Do not run until schema, evals, and baseline reports are ready.
backend: unsloth
method: qlora
base_model: TODO
dataset_path: data/train/TODO.jsonl
output_dir: outputs/unsloth-lora-TODO

lora:
  r: 16
  alpha: 16
  dropout: 0.05
  target_modules:
    - q_proj
    - k_proj
    - v_proj
    - o_proj

training:
  epochs: 1
  learning_rate: 0.0002
  max_seq_length: 2048
  batch_size: 2
  gradient_accumulation_steps: 8
